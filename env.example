SHADOW_DATABASE_URL=postgres://${PGUSER}:${PGPASSWORD}@${PGHOST_DEV}/shadow_db
DIRECT_URL=postgres://${PGUSER}:${PGPASSWORD}@${PGHOST_DEV}/${PGDATABASE}?connect_timeout=10
DATABASE_URL=postgres://${PGUSER}:${PGPASSWORD}@${PGHOST_DEV}/${PGDATABASE}?pgbouncer=true&connect_timeout=10

PGHOST_DEV=   #fill these by setup a postgres database 
PGHOST_PROD=   
PGPASSWORD=   
PGUSER=   
PGDATABASE=   
